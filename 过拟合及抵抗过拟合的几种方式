1.过拟合：代价函数等于或大致等于0，在训练级效果很好但是在测试集效果不理想，实际应用也不理想。
2.抵抗过拟合的方式：
1）增大数据集
2）early stopping :在训练模型的时候，我们往往设置一个比较大的迭代次数，early stopping便是一种提前结束训练的策略用来防止过拟合。一般的做法是记录到目前为止最好的validation accuracy,当连续十个epoch没有达到最佳的accuracy时，则可以认为精确度不会再提高了，此时便可以停止迭代了。
3）dropout  每次训练随机选取一些神经元不工作，也就不更新权值。
4) 正则化项